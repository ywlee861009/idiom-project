# 🚀 App Update Log: v1.0.6 (10)

- **Date**: 2026-03-16
- **Version**: 1.0.6
- **Code**: 10
- **Artifact**: `idiom_v1.0.6_10.aab`

## 💎 주요 변경 사항

### 1. 연속 학습일수(Streak) 시스템 정상화
- **근본 원인 해결**: `UserStatsRepositoryImpl`에서 누락되었던 스트릭 계산 로직을 완벽하게 구현했습니다.
- **연속 학습 판정**: 퀴즈 완료 시 마지막 학습일과 현재 날짜를 비교하여, 연속 학습 여부를 판별하고 스트릭을 유지하거나 갱신합니다.
- **최대 기록 관리**: 현재 스트릭이 역대 최고 기록을 경신할 경우 `maxStreak`도 함께 실시간으로 업데이트됩니다.

### 2. KMP 전용 날짜 유틸리티 (`DateUtils`) 구축
- **멀티플랫폼 대응**: Android 및 공통(Common) 환경에서 날짜를 안정적으로 계산하기 위해 `expect/actual` 구조의 `DateUtils`를 설계했습니다.
- **Epoch Day 기반 정밀도**: 타임존 오프셋을 고려한 Epoch Day 계산 방식을 채택하여, 기기의 시간 변경이나 타임존 이동 시에도 정확한 날짜 차이를 계산합니다.

### 3. 데이터 정합성 및 안정성 강화
- **원자적 상태 업데이트**: DataStore의 `edit` 트랜잭션 내에서 모든 통계(스트릭, 레벨, XP, 마지막 학습일)가 한꺼번에 업데이트되도록 하여 데이터 불일치를 방지했습니다.
- **빌드 검증**: Android 릴리즈 번들(.aab) 빌드 성공 및 서명 검증을 완료했습니다.

---
*Build-Verified by Android Dev Kero*
